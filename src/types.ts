export type Credentials = {
  email: string;
  password: string;
};

export type CurrentUserData = {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
  role: string;
  tenantId: number;
};
